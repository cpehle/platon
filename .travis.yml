language: c
sudo: false
before_install:
    - wget https://raw.github.com/ocaml/opam/master/shell/opam_installer.sh -O - | sh -s "${HOME}/bin"
    - opam init --yes --comp '4.02.3'
    - eval $(opam config env)